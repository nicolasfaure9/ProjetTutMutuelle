{% extends "layout.html.twig" %}
{% set Mutuelle = true %}

{% block title %}Remboursements{% endblock %}

{% block content %}
<h2 class="text-center">{{ block('title') }}</h2>
{% if adhesion %}
    <div class="table-responsive">
        <table class="table table-hover table-condensed">
            <thead>
                <tr>
                    <th>Numero</th>
                    <th>Numero Beneficciaire</th>
                    <th>Acte</th>
                    
                </tr>
            </thead>
            {% for prestation_sante in adhesion.prestations_Details %}
                <tr>
                    
                    
                    
                    <td>{{ prestation_sante.num_sinistre }}</a></td>
                    
                    <td><a href="/remboursements/{{prestation_sante.num_benificiaire_sinistre }}">{{ prestation_sante.num_benificiaire_sinistre }}</a></td>
                    
                    
                    <td>{{ prestation_sante.designation_acte }}</td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% else %}
    <div class="alert alert-warning">Erreur chargement de vos remboursements.</div>
{% endif %}   
{% endblock %}{# empty Twig template #}
